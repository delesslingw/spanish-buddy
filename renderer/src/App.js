import Home from "./Home";

function App() {
    return (
        <div className="App" style={{ backgroundColor: "#fcfcfc", minHeight: "100vh", minWidth: "100vw" }}>
            <Home />
        </div>
    );
}

export default App;
